import { handlePushstate } from "../handlePushstate";

export const searchResultRender = (books1) => {
    const resultsEl = document.querySelector('.input-group');
    const booksEl = document.createElement('ul');
    booksEl.className = 'books1';
}

resultsEl.innerHTML = '';
resultsEl.classList.add('afterType');
if (books.length !== 0) {
    books.map((books) => {

        const booksLiEl = document.createElement('li');
        booksLiEl.className = 'book';
    });
}